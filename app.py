# import streamlit as st
# from langchain_groq import ChatGroq
# from langchain_community.vectorstores import FAISS
# from langchain_huggingface import HuggingFaceEmbeddings
# from langchain_text_splitters import RecursiveCharacterTextSplitter
# from langchain_core.prompts import PromptTemplate
# from langchain_core.documents import Document
# from langchain_core.runnables import RunnablePassthrough
# from langchain_core.output_parsers import StrOutputParser

# st.set_page_config(
#     page_title="RS InfoHub RAG",
#     page_icon="🇬🇪",
#     layout="centered"
# )

# # ========================
# # API Key validation
# # ========================
# try:
#     GROQ_API_KEY = st.secrets["GROQ_API_KEY"]
# except Exception:
#     st.error("GROQ_API_KEY არ არის დაყენებული Secrets-ში!")
#     st.code('GROQ_API_KEY = "gsk_თქვენი_გასაღები"', language="toml")
#     st.stop()

# if not GROQ_API_KEY or not GROQ_API_KEY.startswith("gsk_"):
#     st.error(f"API Key არასწორი ფორმატია. იწყება: '{GROQ_API_KEY[:8]}...'")
#     st.stop()

# # ========================
# # 1. დოკუმენტები
# # ========================
# def load_local_documents():
#     return [
#         Document(
#             page_content="დამატებული ღირებულების გადასახადის (დღგ) განაკვეთი საქართველოში შეადგენს 18 პროცენტს. დღგ-ის გადამხდელად რეგისტრაცია სავალდებულოა, თუ ბრუნვა აღემატება 100,000 ლარს. დღგ-ით დაბეგვრის ობიექტია საქართველოს ტერიტორიაზე საქონლის მიწოდება, მომსახურების გაწევა და საქართველოში საქონლის იმპორტი.",
#             metadata={"source": "საგადასახადო_კოდექსი_მუხლი_157.txt"}
#         ),
#         Document(
#             page_content="მცირე ბიზნესის სტატუსი შეიძლება მიენიჭოს ფიზიკურ პირს, რომლის კალენდარული წლის განმავლობაში მიღებული ერთობლივი შემოსავალი არ აღემატება 500,000 ლარს. მცირე ბიზნესის სტატუსის მქონე პირი იხდის 1%-იან გადასახადს მიღებულ ბრუნვაზე. სტატუსი გაიცემა საგადასახადო ორგანოს მიერ.",
#             metadata={"source": "მცირე_ბიზნესის_რეგულაციები.txt"}
#         ),
#         Document(
#             page_content="საქონლის დეკლარირება და საბაჟო პროცედურები ხორციელდება დეკლარაციის წარდგენით. საბაჟო გამშვები პუნქტები მუშაობენ 24-საათიან რეჟიმში. საქართველოში საქონლის შემოტანისას გადამხდელი ვალდებულია წარადგინოს სასაქონლო დეკლარაცია. საბაჟო გადასახადი განისაზღვრება საქონლის სასაქონლო კოდის მიხედვით.",
#             metadata={"source": "საბაჟო_ადმინისტრირება.txt"}
#         ),
#     ]

# # ========================
# # 2. RAG სისტემა
# # ========================
# @st.cache_resource
# def setup_rag(_api_key):
#     embeddings = HuggingFaceEmbeddings(
#         model_name="sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
#     )

#     raw_docs = load_local_documents()
#     splitter = RecursiveCharacterTextSplitter(chunk_size=300, chunk_overlap=30)
#     split_docs = splitter.split_documents(raw_docs)

#     vectorstore = FAISS.from_documents(split_docs, embeddings)
#     retriever = vectorstore.as_retriever(search_kwargs={"k": 2})

#     llm = ChatGroq(
#         api_key=_api_key,
#         model_name="llama-3.3-70b-versatile",
#         temperature=0,
#         max_tokens=1024,
#     )

#     prompt = PromptTemplate.from_template(
#         "შენ ხარ საგადასახადო/საბაჟო ასისტენტი. უპასუხე კითხვას მხოლოდ კონტექსტზე დაყრდნობით ქართულ ენაზე.\n\n"
#         "კონტექსტი: {context}\n\n"
#         "კითხვა: {question}\n\n"
#         "პასუხი ჩამოაყალიბე გარკვევით. ბოლოში მიუთითე წყარო და: "
#         "პასუხი მომზადებულია RS InfoHub-ის დოკუმენტების მიხედვით - https://infohub.rs.ge/ka"
#     )

#     def format_docs(docs):
#         return "\n\n".join(f"[{d.metadata['source']}]\n{d.page_content}" for d in docs)

#     chain = (
#         {"context": retriever | format_docs, "question": RunnablePassthrough()}
#         | prompt
#         | llm
#         | StrOutputParser()
#     )

#     return chain, retriever

# # ========================
# # 3. UI
# # ========================
# st.title("RS InfoHub - RAG აგენტი")
# st.caption("საგადასახადო და საბაჟო კითხვებზე პასუხი 3 დოკუმენტის საფუძველზე")

# with st.expander("გამოყენებული დოკუმენტები"):
#     st.markdown("""
# - `საგადასახადო_კოდექსი_მუხლი_157.txt` — დღგ-ის განაკვეთი და რეგისტრაცია
# - `მცირე_ბიზნესის_რეგულაციები.txt` — სტატუსი და ბრუნვის ლიმიტი
# - `საბაჟო_ადმინისტრირება.txt` — დეკლარირება და პროცედურები
#     """)

# st.divider()

# if "messages" not in st.session_state:
#     st.session_state.messages = []

# for msg in st.session_state.messages:
#     with st.chat_message(msg["role"]):
#         st.markdown(msg["content"])

# user_query = st.chat_input("დასვი კითხვა ქართულად... (მაგ: რა არის დღგ-ს განაკვეთი?)")

# if user_query:
#     st.session_state.messages.append({"role": "user", "content": user_query})
#     with st.chat_message("user"):
#         st.markdown(user_query)

#     with st.chat_message("assistant"):
#         try:
#             with st.spinner("პასუხი იძებნება..."):
#                 chain, retriever = setup_rag(GROQ_API_KEY)
#                 answer = chain.invoke(user_query)
#                 source_docs = retriever.invoke(user_query)

#             st.markdown(answer)

#             with st.expander("გამოყენებული Chunk-ები"):
#                 for doc in source_docs:
#                     st.markdown(f"**{doc.metadata['source']}**")
#                     st.caption(doc.page_content)

#         except Exception as e:
#             st.error(f"შეცდომა: {str(e)}")

#     st.session_state.messages.append({"role": "assistant", "content": answer})

import streamlit as st
from langchain_groq import ChatGroq
from langchain_community.vectorstores import FAISS
from langchain_huggingface import HuggingFaceEmbeddings
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_core.prompts import PromptTemplate
from langchain_core.documents import Document
from langchain_core.runnables import RunnablePassthrough, RunnableLambda
from langchain_core.output_parsers import StrOutputParser

st.set_page_config(page_title="RS InfoHub RAG", page_icon="🇬🇪", layout="centered")

# ========================
# API Key validation
# ========================
try:
    GROQ_API_KEY = st.secrets["GROQ_API_KEY"]
except Exception:
    st.error("GROQ_API_KEY არ არის დაყენებული Secrets-ში!")
    st.code('GROQ_API_KEY = "gsk_თქვენი_გასაღები"', language="toml")
    st.stop()

if not GROQ_API_KEY or not GROQ_API_KEY.startswith("gsk_"):
    st.error(f"API Key არასწორი ფორმატია.")
    st.stop()

# ========================
# 1. დოკუმენტები
# ========================
DOCUMENTS = [
    Document(
        page_content=(
            "დამატებული ღირებულების გადასახადი დღგ. "
            "დღგ-ის განაკვეთი საქართველოში შეადგენს 18 პროცენტს. "
            "დღგ-ის გადამხდელად რეგისტრაცია სავალდებულოა თუ წლიური ბრუნვა აღემატება 100000 ლარს. "
            "დღგ-ით დაბეგვრის ობიექტია საქართველოში საქონლის მიწოდება მომსახურების გაწევა და იმპორტი."
        ),
        metadata={"source": "საგადასახადო_კოდექსი_მუხლი_157.txt", "topic": "დღგ"}
    ),
    Document(
        page_content=(
            "მცირე ბიზნესი მცირე ბიზნესის სტატუსი. "
            "მცირე ბიზნესის სტატუსი მიენიჭება ფიზიკურ პირს რომლის წლიური შემოსავალი არ აღემატება 500000 ლარს. "
            "მცირე ბიზნესის სტატუსის მქონე პირი იხდის 1 პროცენტიან გადასახადს ბრუნვაზე. "
            "მცირე ბიზნესის სტატუსი გაიცემა საგადასახადო ორგანოს მიერ."
        ),
        metadata={"source": "მცირე_ბიზნესის_რეგულაციები.txt", "topic": "მცირე ბიზნესი"}
    ),
    Document(
        page_content=(
            "საბაჟო საბაჟო ადმინისტრირება დეკლარირება. "
            "საქონლის დეკლარირება და საბაჟო პროცედურები ხორციელდება დეკლარაციის წარდგენით. "
            "საბაჟო გამშვები პუნქტები მუშაობენ 24 საათიან რეჟიმში. "
            "საქართველოში საქონლის შემოტანისას ვალდებული ხარ წარადგინო სასაქონლო დეკლარაცია. "
            "საბაჟო გადასახადი განისაზღვრება სასაქონლო კოდის მიხედვით."
        ),
        metadata={"source": "საბაჟო_ადმინისტრირება.txt", "topic": "საბაჟო"}
    ),
]

# ========================
# 2. Keyword-based retriever (უფრო ზუსტი ქართულისთვის)
# ========================
KEYWORDS = {
    "საგადასახადო_კოდექსი_მუხლი_157.txt": [
        "დღგ", "18", "100000", "100,000", "დამატებული ღირებულება",
        "რეგისტრაცია", "გადამხდელ", "ბრუნვა", "იმპორტ"
    ],
    "მცირე_ბიზნესის_რეგულაციები.txt": [
        "მცირე ბიზნეს", "500000", "500,000", "1%", "სტატუს",
        "ფიზიკური პირ", "შემოსავალ", "წლიური"
    ],
    "საბაჟო_ადმინისტრირება.txt": [
        "საბაჟო", "დეკლარირება", "დეკლარაცი", "გამშვები",
        "24-საათ", "24 საათ", "სასაქონლო", "შემოტანა", "პუნქტ"
    ],
}

def keyword_retrieve(query: str, top_k: int = 1) -> list[Document]:
    q = query.lower()
    scores = {}
    for doc in DOCUMENTS:
        src = doc.metadata["source"]
        score = sum(1 for kw in KEYWORDS[src] if kw.lower() in q)
        scores[src] = score

    # Sort by score, return top_k docs
    ranked = sorted(DOCUMENTS, key=lambda d: scores[d.metadata["source"]], reverse=True)

    # If best score is 0, return all (no clear match)
    if scores[ranked[0].metadata["source"]] == 0:
        return ranked[:top_k]

    # Return only docs with score > 0, max top_k
    matched = [d for d in ranked if scores[d.metadata["source"]] > 0]
    return matched[:top_k]

# ========================
# 3. RAG chain
# ========================
@st.cache_resource
def get_llm(_api_key):
    return ChatGroq(
        api_key=_api_key,
        model_name="llama-3.3-70b-versatile",
        temperature=0,
        max_tokens=1024,
    )

def build_chain(llm):
    prompt = PromptTemplate.from_template(
        "შენ ხარ საგადასახადო/საბაჟო ასისტენტი RS InfoHub-ისთვის.\n"
        "უპასუხე კითხვას მხოლოდ ქვემოთ მოცემულ კონტექსტზე დაყრდნობით ქართულ ენაზე.\n"
        "თუ კონტექსტში პასუხი არ არის, თქვი: 'ამ კითხვაზე ინფორმაცია არ მოიპოვება ბაზაში.'\n\n"
        "კონტექსტი:\n{context}\n\n"
        "კითხვა: {question}\n\n"
        "პასუხი:\n"
        "(ბოლოში მიუთითე: წყარო: [ფაილის სახელი] და\n"
        "პასუხი მომზადებულია RS InfoHub-ის მიხედვით - https://infohub.rs.ge/ka)"
    )
    return prompt | llm | StrOutputParser()

# ========================
# 4. UI
# ========================
st.title("🇬🇪 RS InfoHub — RAG აგენტი")
st.caption("საგადასახადო და საბაჟო კითხვებზე პასუხი 3 დოკუმენტის საფუძველზე")

with st.expander("📂 გამოყენებული დოკუმენტები"):
    st.markdown("""
- `საგადასახადო_კოდექსი_მუხლი_157.txt` — დღგ-ის განაკვეთი და რეგისტრაცია
- `მცირე_ბიზნესის_რეგულაციები.txt` — სტატუსი და ბრუნვის ლიმიტი
- `საბაჟო_ადმინისტრირება.txt` — დეკლარირება და პროცედურები
    """)

st.divider()

if "messages" not in st.session_state:
    st.session_state.messages = []

for msg in st.session_state.messages:
    with st.chat_message(msg["role"]):
        st.markdown(msg["content"])

user_query = st.chat_input("დასვი კითხვა ქართულად... (მაგ: რა არის დღგ-ს განაკვეთი?)")

if user_query:
    st.session_state.messages.append({"role": "user", "content": user_query})
    with st.chat_message("user"):
        st.markdown(user_query)

    with st.chat_message("assistant"):
        try:
            with st.spinner("პასუხი იძებნება..."):
                # Retrieve relevant docs
                retrieved_docs = keyword_retrieve(user_query, top_k=1)
                context = "\n\n".join(
                    f"[{d.metadata['source']}]\n{d.page_content}" for d in retrieved_docs
                )

                # Generate answer
                llm = get_llm(GROQ_API_KEY)
                chain = build_chain(llm)
                answer = chain.invoke({"context": context, "question": user_query})

            st.markdown(answer)

            with st.expander("🔍 გამოყენებული წყარო"):
                for doc in retrieved_docs:
                    st.markdown(f"**📄 {doc.metadata['source']}** (თემა: {doc.metadata['topic']})")
                    st.caption(doc.page_content[:300] + "...")

        except Exception as e:
            st.error(f"შეცდომა: {str(e)}")
            answer = "შეცდომა მოხდა."

    st.session_state.messages.append({"role": "assistant", "content": answer})
